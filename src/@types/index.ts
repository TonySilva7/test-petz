import { IError } from './error';
import { IStatusRequest, IApiPokemon, IResult } from './api';
import { IStateStyles, IMainStyles, IStylesProps } from './styles';

export type {
  IError,
  IStatusRequest,
  IStateStyles,
  IMainStyles,
  IStylesProps,
  IApiPokemon,
  IResult,
};
